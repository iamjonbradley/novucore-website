{
  "name": "novucore-website",
  "version": "1.0.0",
  "private": true,
  "description": "NovuCore — modern professional website for a custom-code web dev studio.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && yarn install --network-timeout 300000 && yarn build"
  }
}
